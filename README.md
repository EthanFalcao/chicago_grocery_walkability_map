# Chicago Grocery Walkability Map

Static GitHub Pages version. Upload `index.html` at the repository root and set Pages to deploy from the `main` branch `/` root. No API key or GitHub Actions required for this static version.
